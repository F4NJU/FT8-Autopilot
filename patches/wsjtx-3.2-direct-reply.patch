diff --git a/widgets/mainwindow.h b/widgets/mainwindow.h
--- a/widgets/mainwindow.h
+++ b/widgets/mainwindow.h
@@ -1048,6 +1048,9 @@
   // dedupe=true only on the two decode-ARRIVAL paths (see processMessage).
   void processMessage(DecodedText const& message, Qt::KeyboardModifiers = Qt::NoModifier,
                       bool dedupe = false);
+  bool isNetworkReplyAllowed (DecodedText const& message, QString const& mode,
+                              bool low_confidence) const;
+  bool m_networkTxDfPending {false};
   void replyToCQ (QTime, qint32 snr, float delta_time, quint32 delta_frequency, QString const& mode, QString const& message_text, bool low_confidence, quint8 modifiers);
   void locationChange(QString const& location);
   void replayDecodes ();
diff --git a/widgets/mainwindow.cpp b/widgets/mainwindow.cpp
--- a/widgets/mainwindow.cpp
+++ b/widgets/mainwindow.cpp
@@ -14653,10 +14653,34 @@
 }
 
 
-// Takes a decoded CQ line and sets it up for reply
+bool MainWindow::isNetworkReplyAllowed (DecodedText const& message, QString const& mode,
+                                        bool low_confidence) const
+{
+  auto recipient = message.call ();
+  auto const is_cq_or_qrz = recipient == "CQ" || recipient == "QRZ"
+    || recipient == "CQDX" || recipient.startsWith ("CQ ");
+  if (is_cq_or_qrz)
+    {
+      return true;              // Preserve the documented Reply behavior.
+    }
+
+  // Direct Reply is deliberately limited to native structured FT8/FT4
+  // messages whose recipient is this configured WSJT-X station.
+  recipient.remove ('<').remove ('>');
+  auto const addressed_to_me = Radio::is_callsign (recipient)
+    && (recipient == m_config.my_callsign ()
+        || recipient == m_baseCall
+        || Radio::base_callsign (recipient) == m_baseCall);
+  return !low_confidence
+    && (mode == "~" || mode == "+")
+    && message.isStandardMessage ()
+    && addressed_to_me;
+}
+
+// Takes an exact decoded CQ/QRZ or message addressed to us and sets it up for reply
 void MainWindow::replyToCQ (QTime time, qint32 snr, float delta_time, quint32 delta_frequency
                             , QString const& mode, QString const& message_text
-                            , bool /*low_confidence*/, quint8 modifiers)
+                            , bool low_confidence, quint8 modifiers)
 {
   QString format_string {"%1 %2 %3 %4 %5 %6"};
   auto const& time_string = time.toString ("~" == mode || "&" == mode || "+" == mode || "*" == mode
@@ -14693,6 +14717,13 @@
     }
   if (!cursor.isNull ())
     {
+      DecodedText message {message_line};
+      if (!isNetworkReplyAllowed (message, mode, low_confidence))
+        {
+          qDebug () << "process reply message ignored, decode is not CQ/QRZ or addressed to this station:"
+                    << message_line;
+          return;
+        }
       if (m_config.udpWindowToFront ())
         {
           show ();
@@ -14704,12 +14735,9 @@
           showNormal ();
           raise ();
         }
-      if ((text.contains (QRegularExpression {R"(^(CQ |CQDX |QRZ ))"}))
-          || text.contains("73 ") || (ui->cbHoldTxFreq->isChecked ())) {
-        // a message we are willing to accept and auto reply to
-        m_bDoubleClicked = true;
-      }
-      DecodedText message {message_line};
+      // Follow the same native path as a user double-click on the exact Decode.
+      m_bDoubleClicked = true;
       Qt::KeyboardModifiers kbmod {modifiers << 24};
       processMessage (message, kbmod);
+      m_networkTxDfPending = false;
       tx_watchdog (false);
diff --git a/revision_utils.cpp b/revision_utils.cpp
--- a/revision_utils.cpp
+++ b/revision_utils.cpp
@@ -27,7 +27,7 @@
 
 QString revision (QString const& scs_rev_string)
 {
-  return "260818";
+  return "260818 AutoPilot-STX1";
   QString result;
   auto revision_from_scs = revision_extract_number (scs_rev_string);
 
diff --git a/Network/NetworkMessage.hpp b/Network/NetworkMessage.hpp
--- a/Network/NetworkMessage.hpp
+++ b/Network/NetworkMessage.hpp
@@ -531,5 +531,16 @@
  *      Callsigns without a sort order will be valued at zero for sorting purposes
  *      in the hound display.
  *
+ * SetTxDF         In      18                     quint32
+ *                         Id (target unique key) utf8
+ *                         Tx DF                  quint32
+ *
+ *      AutoPilot extension for the Improved 3.2.0 PLUS 260818 build. The
+ *      requested audio Tx frequency is accepted only in FT8/FT4, while not
+ *      transmitting, and within the native Tx frequency control range. A
+ *      successful change follows the native Tx frequency control path and
+ *      emits the normal Status message. Type 17 is already InhibitStatus in
+ *      this exact source release, so this extension uses the next free value.
+ *
  *
  * InhibitStatus  Out      17
@@ -586,4 +597,5 @@ namespace NetworkMessage
       Configure,
       AnnotationInfo,
       InhibitStatus,            // Out 17 — see protocol comment above
+      SetTxDF,                  // In 18 - AutoPilot extension
       maximum_message_type_     // ONLY add new message types
diff --git a/Network/MessageClient.hpp b/Network/MessageClient.hpp
--- a/Network/MessageClient.hpp
+++ b/Network/MessageClient.hpp
@@ -133,4 +133,7 @@ public:
   // this signal is emitted if the server has sent information about a callsign
   Q_SIGNAL void annotation_info (QString const& dx_call, bool sort_order_provided, quint32 sort_order);
 
+  // AutoPilot extension: request an explicit audio Tx frequency.
+  Q_SIGNAL void set_tx_df (quint32 tx_df);
+
   // this signal is emitted when network errors occur or if a host
diff --git a/Network/MessageClient.cpp b/Network/MessageClient.cpp
--- a/Network/MessageClient.cpp
+++ b/Network/MessageClient.cpp
@@ -407,4 +407,16 @@ void MessageClient::impl::parse_message (QByteArray const& msg)
             }
             break;
 
+            case NetworkMessage::SetTxDF:
+              {
+                quint32 tx_df {0};
+                in >> tx_df;
+                TRACE_UDP ("SetTxDF tx df:" << tx_df);
+                if (check_status (in) != Fail)
+                  {
+                    Q_EMIT self_->set_tx_df (tx_df);
+                  }
+              }
+              break;
+
             default:
diff --git a/widgets/mainwindow.cpp b/widgets/mainwindow.cpp
--- a/widgets/mainwindow.cpp
+++ b/widgets/mainwindow.cpp
@@ -921,3 +921,19 @@ MainWindow::MainWindow(QDir const& temp_directory, bool multiple,
     });
   connect (m_messageClient, &MessageClient::reply, this, &MainWindow::replyToCQ);
+  connect (m_messageClient, &MessageClient::set_tx_df, [this] (quint32 tx_df) {
+      if (m_transmitting
+          || (m_mode != "FT8" && m_mode != "FT4")
+          || tx_df < quint32 (ui->TxFreqSpinBox->minimum ())
+          || tx_df > quint32 (ui->TxFreqSpinBox->maximum ()))
+        {
+          qDebug () << "SetTxDF ignored, invalid state or frequency:" << tx_df;
+          return;
+        }
+      // setValue uses the native handler: waterfall marker, audio/XIT,
+      // band checks, settings and normal Status publication all stay coherent.
+      m_networkTxDfPending = true;
+      ui->TxFreqSpinBox->setValue (int (tx_df));
+      statusUpdate ();          // also acknowledge an unchanged requested DF
+      QTimer::singleShot (10000, this, [this] {m_networkTxDfPending = false;});
+    });
   connect (m_messageClient, &MessageClient::close, this, &MainWindow::close);
@@ -10128,4 +10144,5 @@ void MainWindow::processMessage (DecodedText const& message, Qt::KeyboardModifiers modifiers,
       if (((SpecOp::HOUND != m_specOp) || m_mode != "FT8")
-          && (!ui->cbHoldTxFreq->isChecked () || shift || ctrl)) {
+          && !m_networkTxDfPending
+          && (!ui->cbHoldTxFreq->isChecked () || shift || ctrl)) {
         ui->TxFreqSpinBox->setValue(frequency);
       }
